<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Every variant must equal the surface the app draws with, so the window shows the app's own
         colour before the first frame and behind the system bars. -->
    <color name="window_background">#F0FCF8</color>
</resources>
